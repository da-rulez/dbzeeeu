## v0.16.65: Calibration Dimension Cliff Fix [Patch]

Fixes broken-looking terrain in the Calibration Dimension where two cliffs overlap. A lower cliff layer was rendering over the level above it, so rock faces poked through the upper level's grass. Overlapping cliff layers now keep their correct top-to-bottom order again, so the multi-level terrain reads properly.
