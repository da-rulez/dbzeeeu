## v0.16.57: Legacy Map Migration [Patch]

Phase 6 of the map toolkit: the old Tiled maps now convert into the new system.

- All three legacy maps (the overworld, Muscle Tower, and the calibration zone) convert automatically with PROVEN collision equivalence: every one of the old pipeline's collision rects is covered, with zero phantom collision, verified by an automated suite.
- The calibration zone now runs on the new system live (pilot). The converted overworld is warpable as "MK Solum Bloom HQ" for side-by-side comparison; the live overworld is unchanged until it passes eyes-on review.
- Measured against the original baseline, the converted overworld beats the old pipeline everywhere: server map memory down 56 percent, draw calls 18 to 8, render CPU halved, movement collision three times cheaper.
