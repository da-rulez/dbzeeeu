## v0.22.57 — Clearer multi-hit authoring [Patch]

**Under the hood**
- A technique can never touch more targets than it has hits, so setting its pierce above its hit budget does nothing. The projectile editor now spells out what any pair of those two settings actually produces, and flags the combination that has no effect.
- Added a guard so editor fields that are built but never saved cannot ship again.
